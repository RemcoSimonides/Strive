import { MilestonePathPipe } from './milestone-path.pipe';

describe('MilestonePathPipe', () => {
  it('create an instance', () => {
    const pipe = new MilestonePathPipe();
    expect(pipe).toBeTruthy();
  });
});
